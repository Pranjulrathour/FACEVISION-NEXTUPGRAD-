# Evaluation- and test-only dependencies. NOT part of the production app --
# deliberately excluded from requirements.txt so the deployed Docker image
# doesn't carry these. Install alongside requirements.txt for local dev or
# CI: pip install -r requirements.txt -r requirements-eval.txt.
#
# numpy/Pillow/onnxruntime/scikit-learn/pandas/pyarrow: backend/evaluation/
# (checklist §4/§22/§23) and its tests. See docs/evaluation-methodology.md.
numpy==2.4.6
Pillow==12.3.0
onnxruntime==1.19.2
scikit-learn==1.9.0
pandas==3.0.5
pyarrow==25.0.1

# fakeredis: in-memory Redis simulator for testing the Redis-backed rate
# limiter (checklist §26, app/core/rate_limit.py) without needing a real
# Redis server in CI/local dev.
fakeredis==2.37.1
